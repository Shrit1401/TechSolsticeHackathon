# DataFrames for Prometheus/Loki parsing and CSV I/O.
pandas>=2.0.0
# HTTP to Prometheus, Loki, Jaeger query APIs.
requests>=2.28.0
# Log line embeddings (SentenceTransformer); pulls PyTorch as transitive dep.
sentence-transformers>=3.0.0
numpy>=1.24.0
# MinMaxScaler in preprocess; optional sklearn utilities elsewhere.
scikit-learn>=1.3.0
# Persist ``MinMaxScaler`` next to ``sequences.npz``.
joblib>=1.3.0
# LSTM and autoencoder training/inference (Keras 3).
tensorflow>=2.15.0
matplotlib>=3.8.0
